interface:
  display_name: "Agent Case Share Personal Retrieval"
  short_description: "Retrieve personal cases, attachments, and assets through MCP."
  default_prompt: "Use $agent-case-share-personal-retrieval to reuse relevant cases, attachments, or assets through MCP."
dependencies:
  tools:
    - type: "mcp"
      value: "agent-case-share"
      description: "Agent Case Share MCP server"
