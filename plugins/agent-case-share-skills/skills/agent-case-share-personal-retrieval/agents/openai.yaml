interface:
  display_name: "Agent Case Share Personal Retrieval"
  short_description: "Proactively retrieve relevant personal cases/assets for the current task."
  default_prompt: "Use $agent-case-share-personal-retrieval when the current task would benefit from the user's prior cases or assets; explicit invocation or a direct reuse request should always trigger retrieval."
