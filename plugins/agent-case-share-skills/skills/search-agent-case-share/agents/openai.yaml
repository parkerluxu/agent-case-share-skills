interface:
  display_name: "Search Agent Case Share"
  short_description: "Search content and case attachments through MCP."
  default_prompt: "Use $search-agent-case-share to find relevant content or case attachments through MCP."
dependencies:
  tools:
    - type: "mcp"
      value: "agent-case-share"
      description: "Agent Case Share MCP server"
