interface:
  display_name: "Search Agent Case Share"
  short_description: "Search content, case videos, and attachments through MCP."
  default_prompt: "Use $search-agent-case-share to find relevant content, case videos, or attachments through MCP."
dependencies:
  tools:
    - type: "mcp"
      value: "agent-case-share"
      description: "Agent Case Share MCP server"
