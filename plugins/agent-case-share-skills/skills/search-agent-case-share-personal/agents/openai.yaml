interface:
  display_name: "Search My Agent Case Share"
  short_description: "Search personal cases, attachments, and assets through MCP."
  default_prompt: "Use $search-agent-case-share-personal to find my cases, attachments, and assets through MCP."
dependencies:
  tools:
    - type: "mcp"
      value: "agent-case-share"
      description: "Agent Case Share MCP server"
