interface:
  display_name: "Agent Case Share"
  short_description: "Publish content, case videos, and attachments through MCP."
  default_prompt: "Use $publish-agent-case-share to publish a case or manage its videos and attachments through MCP."
dependencies:
  tools:
    - type: "mcp"
      value: "agent-case-share"
      description: "Agent Case Share MCP server"
